export const XP_RATES = {
  PER_PAGE: 2,        // XP per page read
  PER_CHAPTER: 2,     // XP per chapter (Keep same as page if 1 chapter = 1 page)
  PER_MINUTE: 1,      // XP per minute spent reading
  BOOK_COMPLETION: 500, // XP for finishing a book
};

export const ACHIEVEMENTS = {
  LONG_SESSION_MINUTES: 60,
  MARATHON_SESSION_MINUTES: 180,
};
